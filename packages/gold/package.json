{
  "name": "gold",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "generate": "yarn sponsorkit --filter='<1600' --name=sponsors.gold"
  },
  "dependencies": {
    "sponsorkit": "^0.9.1"
  }
}
