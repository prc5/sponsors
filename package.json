{
  "name": "sponsors",
  "version": "0.0.0",
  "license": "MIT",
  "scripts": {
    "install": "yarn install-sharp",
    "install-sharp": "npm install sharp --no-save --ignore-engines --force -W -D",
    "generate": "nx run-many --target=generate --all"
  },
  "private": true,
  "devDependencies": {
    "@nx/js": "18.0.7",
    "@nx/plugin": "18.0.7",
    "nx": "18.0.7",
    "sponsorkit": "0.14.0"
  },
  "workspaces": [
    "packages/*"
  ]
}
